import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center'
  },

  label: {
    textAlign: 'center',
    fontSize: 16,
    color: '#333'
  }
});
